apiVersion: v1
kind: Service
metadata:
  name: fireserve-gateway
  labels:
    app: fireserve
    component: gateway
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: fireserve
    component: gateway
